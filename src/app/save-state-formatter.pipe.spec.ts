import { SaveStateFormatterPipe } from './save-state-formatter.pipe';

describe('SaveStateFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SaveStateFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
